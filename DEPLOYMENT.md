# 🤖 Бот Анонімних Повідомлень - Інструкції Розгортання

## 📋 Вимоги

- Telegram Bot Token (від BotFather)
- Telegram User ID (твій ID)
- Supabase проект з таблицею `survey_responses`
- Render.com акаунт (для хостинга) або локальна машина для тестування

## 🗄️ Supabase Налаштування

### 1. Таблиця вже створена в проекті: `lypsnqhpwdhemguuqgqi`

Якщо потрібно пересоздати таблицю, виконайте цей SQL запит у Supabase SQL Editor:

```sql
-- Опціонально: видалити існуючу таблицю (ДУЖЕ ОБЕРЕЖНО!)
DROP TABLE IF EXISTS survey_responses;

-- Створити таблицю для результатів опитування
CREATE TABLE survey_responses (
    id BIGSERIAL PRIMARY KEY,
    user_id BIGINT NOT NULL,
    username VARCHAR(255),
    appearance VARCHAR(50),
    intelligence VARCHAR(50),
    character VARCHAR(50),
    kindness VARCHAR(50),
    humor VARCHAR(50),
    would_date VARCHAR(50),
    know_personally VARCHAR(50),
    additional TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 2. Отримати Supabase облікові дані

1. Перейти на https://supabase.com/dashboard
2. Обрати проект `lypsnqhpwdhemguuqgqi`
3. Перейти в **Settings → API**
4. Скопіювати:
   - **Project URL**: `https://lypsnqhpwdhemguuqgqi.supabase.co`
   - **Anon Public Key** (НЕ admin key!)

## 🔐 Отримання Telegram облікових даних

### 1. Bot Token
1. Напишіть **@BotFather** в Telegram
2. Команда `/newbot`
3. Задайте ім'я та username для бота
4. Скопіюйте токен

### 2. Ваш User ID
1. Запустіть бот або напишіть `/start`
2. Запустіть `/iamowner`
3. Побачите ваш ID у вигляді `<code>123456789</code>`

## 🐳 Локальне Тестування (Docker)

### 1. Встановити Docker
```bash
# На Ubuntu/Debian
sudo apt-get install docker.io docker-compose

# На macOS (через Homebrew)
brew install docker-compose
```

### 2. Запустити бот локально
```bash
cd /workspaces/TG_BOT_AN

# Заповніть змінні оточення у docker-compose.yml
# Потім запустіть:
docker-compose up --build
```

### Змінні у docker-compose.yml:
```yaml
environment:
  - BOT_TOKEN=YOUR_BOT_TOKEN_HERE
  - OWNER_ID=YOUR_TELEGRAM_ID_HERE
  - SUPABASE_URL=https://lypsnqhpwdhemguuqgqi.supabase.co
  - SUPABASE_KEY=YOUR_SUPABASE_ANON_KEY_HERE
```

## 🚀 Розгортання на Render

### 1. Підготувати GitHub Repository

```bash
cd /workspaces/TG_BOT_AN

# Додати всі файли
git add .

# Зробити коміт
git commit -m "Deploy bot with anonymous messages and survey features"

# Залити на GitHub
git push origin main
```

### 2. Розгорнути на Render.com

1. Перейти на https://render.com
2. Натиснути **+ New** → **Web Service**
3. Обрати **Deploy an existing GitHub repository**
4. Знайти і обрати репозиторій `TG_BOT_AN`

### 3. Налаштування на Render

**Основні налаштування:**
- **Name**: `anon-bot` (або яке завгодно ім'я)
- **Root Directory**: (залишити пусто)
- **Runtime**: `Docker`
- **Build Command**: (залишити пусто - Render автоматично знайде Dockerfile)
- **Start Command**: (залишити пусто - Render використовуватиме CMD з Dockerfile)

**Додати Environment Variables:**

| Name | Value |
|------|-------|
| `BOT_TOKEN` | Ваш токен від BotFather |
| `OWNER_ID` | Ваш Telegram ID |
| `SUPABASE_URL` | `https://lypsnqhpwdhemguuqgqi.supabase.co` |
| `SUPABASE_KEY` | Ваш Supabase anon key |

### 4. Запустити розгортання

Натиснути **Create Web Service** і чекати, поки Render збере й розгорне бот.

Лог розгортання можна переглядати в **Logs** секції.

## 📝 Команди Бота

### Для звичайних користувачів:
- **📋 Анкета** - Пройти опитування (8 питань про особистість)
- **💬 Анонімне повідомлення** - Надіслати анонімне повідомлення власнику

### Для власника:
- **/start** - Меню власника
- **📋 Анкета** - Пройти свою анкету
- **📊 Результати** - Переглянути всі відповіді з Supabase
- **🧹 Очистити** - Очистити локальні дані
- **❓ Питання** - Управління питанням
  - **➕ Нове питання** - Встановити нове питання
  - **❌ Очистити** - Видалити питання

## 🎯 Особливості Бота

✅ **Анонімні повідомлення** - Користувачи можуть надсилати повідомлення анонімно, власник бачить лише username

✅ **Опитування з 8 параметрів:**
- 👤 Зовнішність (1-10)
- 🧠 Ум (1-10)
- 🎭 Характер (1-10)
- ❤️ Доброта (1-10)
- 😄 Чувство юмору (1-10)
- 💑 Готовність зустрічатися (Да/Нет)
- 🤝 Знайомі особисто (Да/Нет)
- ✍️ Додаткове повідомлення (текст)

✅ **Кнопки управління:**
- ⏭️ Пропустити будь-яке питання
- 🛑 Скасувати анкету
- ✅ Надіслати анкету

✅ **Збереження в Supabase** - Всі відповіді автоматично зберігаються в базі даних

✅ **Налаштування питань** - Власник може змінювати/видаляти питання в будь-який час

## 🛠️ Локальна Розробка

### Компіляція
```bash
cd /workspaces/TG_BOT_AN
make
```

### Запуск локально (БЕЗ Docker)
```bash
# Встановіть змінні оточення
export BOT_TOKEN="your_token"
export OWNER_ID="your_id"
export SUPABASE_URL="https://lypsnqhpwdhemguuqgqi.supabase.co"
export SUPABASE_KEY="your_key"

# Запустіть бот
./bot
```

### Очистити компільовані файли
```bash
make clean
```

## 📊 Переглід Результатів Опитування

### Через бот (Власнику):
1. Натиснути **📊 Результати**
2. Бот покаже всі відповіді з Supabase

### Через Supabase Dashboard:
1. Перейти https://supabase.com/dashboard
2. Обрати проект `lypsnqhpwdhemguuqgqi`
3. Перейти в **SQL Editor**
4. Запит для усіх результатів:
```sql
SELECT * FROM survey_responses ORDER BY created_at DESC;
```

## ⚠️ Важливо!

1. **НІКОЛИ** не комітьте `.env` файли з реальними ключами
2. Використовуйте **anon public key** з Supabase, НЕ admin key
3. Сохраняйте ваші токени в безпеці
4. На Render використовуйте Environment Variables, не hardcoded values

## 🐛 Розв'язання Проблем

### Бот не реагує на команди
- Перевірте, що BOT_TOKEN встановлений правильно
- Перевірте логи в Render або локально

### Повідомлення не зберігаються
- Перевірте SUPABASE_KEY та SUPABASE_URL
- Переконайтесь, що таблиця `survey_responses` існує

### Docker помилка при збиранні
- Переконайтесь, що встановлені: `g++`, `libcurl4-openssl-dev`, `nlohmann-json3-dev`
- Перезавантажте Docker: `docker system prune -a`

## 📞 Контакти Підтримки

Для питань та проблем зв'яжіться з розробником або див. логи розгортання.

---

**Версія**: 1.0  
**Остання оновлення**: Листопад 2025  
**Статус**: Готово до запуску
